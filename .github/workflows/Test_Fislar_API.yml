name: Test Filstar API

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test Filstar full response
        run: |
          echo "=== URL ==="
          echo "https://filstar.com/api/products?page=1&limit=1000"
          echo "=== Response body ==="
          curl -s \
            -H "Authorization: Bearer 945135" \
            -H "Content-Type: application/json" \
            "https://filstar.com/api/products?page=1&limit=1000"
