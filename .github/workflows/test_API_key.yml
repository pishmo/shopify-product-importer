name: Test Basic Connection

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test basic internet
        run: |
          STATUS=$(curl -s -o /dev/null -w "%{http_code}" https://google.com)
          echo "Google status: $STATUS"
      
      - name: Get my IP
        run: |
          curl https://api.ipify.org
